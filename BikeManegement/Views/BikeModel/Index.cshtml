@model IEnumerable<BikeManegement.Models.BikeModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<br />
<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-info">Models of Vehicle</h2>
    </div>
    <div class="col-6 text-right">
        <a class="btn btn-info" asp-controller="BikeModel" asp-action="Create"><i class="fas fa-motorcycle"></i> &nbsp; Add New Model</a>
    </div>
    <br />
    <div class="col-12">
        <table class="table table-striped border">
            <tr class="table-info">
                <th>
                    @Html.DisplayNameFor(m => m.BikeMaker.MakerName)
                </th>

                <th>
                    @Html.DisplayNameFor(m => m.ModelName)
                </th>
                <th></th>
            </tr>
            @foreach (var item in Model)
            {
        <tr>
            <td>
                @Html.DisplayFor(m => item.BikeMaker.MakerName)
            </td>

            <td>
                @Html.DisplayFor(m => item.ModelName)
            </td>


            <!--<td class="text-right">
                <div class="btn-group" role="group">-->
                    @*<a type="button" class="btn btn-primary" href="@Url.Action("Edit/" + item.Id)">Edit</a>*@


                    <!--<a type="button" class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id"> <i class="far fa-edit"></i></a>


                    <form action="delete" method="post">
                        <button onclick="if(!confirm('Are you sure you want to delete this Bike Model'))
                               {return false}"
                                type="submit" class="btn btn-danger rounded-0" method="post" asp-action="Delete" asp-route-id="@item.Id">
                            <i class="far fa-trash-alt"></i>
                        </button>
                    </form>
                </div>
            </td>-->


            <partial name="_Tablebuttonpartial" model="item.Id" />
        </tr>
            }

        </table>
    </div>
</div>

